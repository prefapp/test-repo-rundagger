name: Dagger magic
on:
  workflow_dispatch:
jobs:
  call-action:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Call composite action
        uses: prefapp/run-dagger-py@main
        with:
          workflow: local_pr_verify
          config_file: .github/firestarter.yml
          pyproject_path: .dagger
          vars: |
            foo="bar"
          secrets: |
            secret1="topsecret"
